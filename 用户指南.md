# IDE Inspector — 用户指南

在 IntelliJ 系列 IDE 中快速查找代码检查类(Inspection)的实现类并浏览其 Git 历史的插件。

## 目录

1. [安装](#安装)
2. [快速开始](#快速开始)
3. [内联代码检查透镜](#内联代码检查透镜)
4. [导出意图和检查](#导出意图和检查)
5. [查看检查的 Git 历史](#查看检查的-git-历史)
6. [设置和配置](#设置和配置)
7. [工具窗口：IDE 属性](#工具窗口ide-属性)
8. [MCP 集成](#mcp-集成)

---

## 安装

### 从 JetBrains Marketplace 安装

1. 打开 IntelliJ IDEA（或其他 JetBrains IDE）。
2. 进入 **Settings | Plugins**（macOS 上为 **Preferences | Plugins**）。
3. 搜索 **IDE Inspector**。
4. 点击 **Install** 并重启 IDE。

### 从本地磁盘安装

1. 从 [GitHub Releases](https://github.com/beansoft/IntelliJ-IDE-Inspector/releases) 下载 `.zip` 插件包。
2. 打开 IDE。
3. 进入 **Settings | Plugins**（或 **Preferences | Plugins**）。
4. 点击 **⚙️ | Install plugin from disk...**。
5. 选择下载的 `.zip` 文件。
6. 重启 IDE。

### 支持的 IDE

- **IntelliJ IDEA 2026.1+**（终极版 Ultimate）
- **WebStorm 2026.1+**
- **PyCharm 2026.1+**
- **CLion 2026.1+**
- **Rider**：不支持（平台差异）

---

## 快速开始

### 三种查找检查类的方式：

1. **直接点击编辑器中的检查结果**。
2. **菜单：Help | Dump Editor Intentions**，查看当前行的所有意图和检查。
3. **使用 IDE 属性工具窗口**（见下文），全局浏览所有检查。

复制检查/意图的类名，然后使用 **Help | Show Git Log For Classes...**（需要在另一个 IDE 窗口打开 IntelliJ Community 源码仓库）。

---

## 内联代码检查透镜

### 什么是内联透镜？

插件默认在有错误、警告等检查的代码行旁显示小的、可点击的标记（透镜）。这样可以直观地看到哪些检查适用于某一行。

### 默认显示的检查严重级别

- 错误(Error)
- 警告(Warning)
- 弱警告(Weak Warning)
- 服务器问题(Server Problem)
- 语法错误(Grammar Error)
- 拼写错误(Typo)
- 其他高级别的检查

### 点击透镜

点击任何透镜标记打开一个对话框，显示：
- 检查的**类名**（例如：`com.intellij.codeInspection.unused.UnusedDeclarationInspection`）
- 相关的快速修复**操作类名**（例如：`com.intellij.codeInspection.unused.RemoveUnusedVariableFix`）

复制类名，粘贴到 **Help | Show Git Log For Classes...** 中跳转到源代码。

---

## 导出意图和检查

### 通过菜单导出

1. 在编辑器中任意一行点击。
2. 进入 **Help | Dump Editor Intentions**。
3. 对话框显示该光标位置的所有可用意图、检查和快速修复。
4. 每项都显示：
   - 人类可读的描述
   - 实现类名（可复制）

### 通过点击透镜导出

点击任何内联透镜标记，可以看到该位置的特定检查及其快速修复。

---

## 查看检查的 Git 历史

> **前提条件**：必须配置本地 `intellij-community` 仓库检出的路径，此功能才能工作。

### 1. 配置 IDEA 源码仓库路径

1. 进入 **Settings | Tools | IDE Inspector**（macOS 上为 **Preferences | Tools | IDE Inspector**）。
2. 在 **IDEA Source Repository** 下，点击 **Browse...** 并选择本地 `intellij-community` 检出目录。
   - 例如：`/path/to/intellij-community/`
3. 点击 **OK** 保存。

### 2. 打开 IDE 属性工具窗口

1. 进入 **View | Tool Windows | IDE Properties**。
2. 点击工具窗口中的 **Inspections** 标签。
3. 一个按类别分组的检查树视图显示所有可用的检查。

### 3. 选择检查并查看其 Git 历史

1. 在 **Inspections** 标签中，在树中找到并点击一个检查。
2. 点击工具窗口工具栏中的 **Show Git History** 按钮（历史图标）。
3. 弹窗显示影响该检查源文件的最近提交：
   - **Date** — 提交时间
   - **Author** — 提交作者
   - **Hash** — 提交哈希值（8 个字符，悬停可显示完整哈希）
   - **Subject** — 提交信息

### 弹窗操作

- **双击一行** → 复制完整提交哈希到剪贴板
- **右键点击一行** → 上下文菜单：
  - **Copy Hash** — 复制到剪贴板
  - **Open in GitHub** — 在 GitHub 中打开（需网络连接）
- **底部按钮**：
  - **Copy Path** — 复制源文件路径到剪贴板
  - **Open File** — 在新编辑器标签页中打开源文件

---

## 设置和配置

### 访问设置

1. 进入 **Settings | Tools | IDE Inspector**（macOS 上为 **Preferences | Tools | IDE Inspector**）。

### 选项

| 设置 | 说明 | 默认值 |
|------|------|--------|
| **IDEA Source Repository** | 本地 `intellij-community` 检出的路径。查看 Git 历史时必需。 | （空） |
| **Visible Severities** | 选择在代码编辑器中显示为内联透镜的检查严重级别。 | 错误、警告、弱警告等 |

### 配置可见的严重级别

1. 在 **Settings | Tools | IDE Inspector** 中，找到 **Visible Severities** 部分。
2. 勾选或取消勾选严重级别以显示/隐藏内联透镜：
   - **Error** — 严重错误
   - **Warning** — 潜在问题
   - **Weak Warning** — 轻微问题
   - **Server Problem** — IDE 服务器问题
   - **Grammar Error** — 语法/拼写检查问题
   - **Typo** — 拼写错误检测
   - 插件定义的自定义严重级别

3. 点击 **OK** 应用更改。

---

## 工具窗口：IDE 属性

**IDE Properties** 工具窗口显示插件相关信息和检查浏览功能。

### 打开

1. 进入 **View | Tool Windows | IDE Properties**。

### 标签

#### Inspections 标签

- 按类别（例如 Java、Kotlin、XML）分组的所有可用检查的**树视图**。
- **选择一个检查** → 其类名在状态栏中显示。
- **点击工具栏上的"Show Git History"按钮** → 打开弹窗显示最近提交（见 [查看检查的 Git 历史](#查看检查的-git-历史)）。

#### Special Paths 标签

- **浏览 IDE 的特殊路径**（系统路径、用户路径、插件路径等）。
- 对调试或了解 IDE 目录结构很有用。
- 路径包括：
  - 系统配置目录
  - 用户插件目录
  - IDE 安装目录
  - 项目目录
  - 等等

---

## MCP 集成

> **说明**：当插件运行在支持 JetBrains MCP Server 插件的 IDE 中时，MCP（模型上下文协议）工具可用（IDE 2024.2+ 中内置）。

### 什么是 MCP？

MCP 是一种协议，允许外部客户端（如 Claude Code、语言模型或其他工具）通过结构化接口向 IDE 请求操作。

### IDE Inspector 提供的 MCP 工具

插件通过 MCP 暴露以下工具：

#### 1. `show_file_history`

在 IDE 的 Version Control 工具窗口中打开文件的 Git 历史。

**参数：**
- `filePath`（字符串）：文件的绝对路径或项目相对路径。

**示例：**
```
工具：show_file_history
输入：filePath = "/Users/you/intellij-community/platform/platform-api/src/com/intellij/openapi/project/Project.java"
```

**结果：** 打开 **Version Control** 工具窗口，显示 `Project.java` 的提交历史。

#### 2. `show_file_history_for_fqn`

通过类的完全限定名 (FQN) 解析并在 IDE 的 Version Control 工具窗口中打开其 Git 历史。需要配置 **IDEA Source Repository** 路径。

**参数：**
- `fqn`（字符串）：完全限定类名（例如：`com.intellij.openapi.project.Project`）。

**示例：**
```
工具：show_file_history_for_fqn
输入：fqn = "com.intellij.openapi.project.Project"
```

**结果：**
1. 解析类在配置的 `intellij-community` 检出中的源文件。
2. 打开 **Version Control** 工具窗口，显示该文件的提交历史。

#### 3. `show_git_log_for_classes`

在 IDE 的 Version Control 工具窗口中打开多个类的 Git 日志。

**参数：**
- `classes`（字符串数组）：完全限定类名列表。

**示例：**
```
工具：show_git_log_for_classes
输入：classes = [
  "com.intellij.codeInspection.unused.UnusedDeclarationInspection",
  "com.intellij.codeInspection.naming.NamingConventionInspection"
]
```

**结果：** 打开 **Version Control** 工具窗口，显示两个类的合并历史。

### 在 Claude Code 中使用 MCP

如果使用支持 JetBrains MCP Server 集成的 Claude Code：

1. **配置 IDEA Source Repository 路径**（见 [设置](#设置和配置)）。
2. **Claude Code 自动检测**可用的 MCP 工具。
3. **当 Claude 需要展示检查源代码历史时**，会使用 `show_file_history_for_fqn` 直接跳转到正确的提交。

---

## 故障排除

### 透镜不显示

- **检查设置**：进入 **Settings | Tools | IDE Inspector** 并验证严重级别没有全部取消勾选。
- **重新加载 IDE**：有时需要完全重启。
- **检查文件类型**：透镜只在有检查的编辑器中显示（例如源代码文件）。

### Git 历史找不到

- **配置路径**：进入 **Settings | Tools | IDE Inspector** 并设置 **IDEA Source Repository** 路径。
- **验证路径**：确保它指向有效的 `intellij-community` 检出，含 `.git` 目录。
- **检查类名**：确保检查类名正确且存在于该仓库中。

### MCP 工具不可用

- **检查 IDE 版本**：MCP 支持需要 IDE 2024.2 或更新版本。
- **验证 MCP Server 插件**：进入 **Settings | Plugins** 并搜索"MCP Server"。它应该被内置并启用。
- **重启 IDE**：对插件依赖的更改有时需要重启。

---

## 提示和技巧

1. **分割 Version Control 工具窗口**：在 **Version Control** 工具窗口中，右键点击 **History** 标签并选择 **Split 'History' Group**。这样可以并排查看多个文件历史。

2. **使用外部 Git 日志**：当 IDEA 源码仓库在单独的 IDE 窗口中打开时，使用 **Help | Show External Git Log For Classes...** 打开其历史，而无需切换窗口。

3. **意图快捷键**：在任何行使用标准 IntelliJ 快捷键（通常为 **Alt+Enter** 或 **Ctrl+Enter**）打开意图菜单。

4. **自定义 Lint 严重级别**：如果某些检查严重级别导致编辑器混乱，在 **Settings | Tools | IDE Inspector** 的 **Visible Severities** 下取消勾选它们。

---

## 支持和反馈

- **GitHub Issues**：[beansoft/IntelliJ-IDE-Inspector](https://github.com/beansoft/IntelliJ-IDE-Inspector/issues)
- **插件页面**：[JetBrains Marketplace](https://plugins.jetbrains.com/plugin/INSERT_PLUGIN_ID)

---

## 致谢

灵感来自并扩展了 [Inspection Lens](https://plugins.jetbrains.com/plugin/19678-inspection-lens)。
